<?php

$chaine = "lanuitondort";
$compteur = 0;

// affichage des chiffres de 0 à la taille de la chaine avec une boucle
while($compteur < strlen($chaine)) {
    echo $compteur;
    $compteur = $compteur + 1;
}


// affichage des chiffres de 0 à la taille de la chaine avec une boucle
while($compteur < strlen($chaine)) {
    echo $compteur;
    $compteur = $compteur + 1;
}

?>